# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# JVM settings - optimized for CI builds
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# Parallel builds - each plugin module compiles concurrently
org.gradle.parallel=true

# Build cache - reuse compilation outputs from previous builds
org.gradle.caching=true

# Configuration cache - disabled globally due to Cloudstream gradle plugin incompatibility
# (DeployWithAdbTask and make tasks use Task.project at execution time)
# CI workflow enables it selectively via --configuration-cache flag
# org.gradle.configuration-cache=true

# AndroidX package structure
android.useAndroidX=true
# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true
